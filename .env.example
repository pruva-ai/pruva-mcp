# Optional: Pruva API URL override (defaults to https://www.pruva.ai)
# Useful for pointing the server at staging without re-running pruva_login.
PRUVA_API_URL=http://localhost:3000

# Optional: HTTP port when running with --http flag (defaults to 3100)
PORT=3100
