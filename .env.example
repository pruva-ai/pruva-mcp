# Required: Your Pruva API key
PRUVA_API_KEY=pruva_xxx

# Optional: Pruva API base URL (defaults to https://app.pruva.io)
PRUVA_BASE_URL=http://localhost:3000

# Optional: HTTP port when running with --http flag (defaults to 3100)
PORT=3100